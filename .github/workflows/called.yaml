name: Called Workflow

on:
  workflow_call:
    inputs:
      input1:
        type: string

jobs:
  called-job1:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "----------"
          echo input1 value is "$INPUT1"
          echo "----------"
        env:
          INPUT1: ${{ inputs.input1 }}
