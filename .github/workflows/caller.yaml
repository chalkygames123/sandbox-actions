name: Caller

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - run: caller > job1 > step1
  job2:
    needs:
      - job1
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/workflows/called.yaml
        with:
          input1: value of input1
