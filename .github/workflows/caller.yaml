name: Caller

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  job1:
    steps:
      - run: echo "job1 > step1"
  job2:
    needs:
      - job1
    steps:
      - uses: ./.github/workflows/called.yaml
        with:
          input1: value of input1
