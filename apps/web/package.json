{
	"name": "web",
	"scripts": {
		"build": "node -e \"fs.cpSync('src', 'dist', { recursive: true }); fs.writeFileSync('stats.html', '')\"",
		"check": "echo \"no check specified\"",
		"generate:types": "echo \"no generate:types specified\"",
		"lint": "echo \"no lint specified\""
	},
	"devDependencies": {
		"@cloudflare/workers-types": "4.20260702.1",
		"hono": "4.12.31",
		"wrangler": "4.114.0"
	}
}
